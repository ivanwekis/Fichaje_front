export class Register {
    date: string;
    start: string;
    finish: string;

    constructor(date: string, start: string, finish: string) {
        this.date = date;
        this.start = start;
        this.finish = finish;
    }
}
